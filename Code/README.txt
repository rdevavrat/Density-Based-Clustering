* dbscan.py is a python script which can be executed from a terminal and takes command line arguments

*   This code accepts three arguments:
           
           1. file path (where your input data is kept)
           2. Epsilon
           3. Minimum points
       
           Press enter to execute.